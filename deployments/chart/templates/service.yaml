apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-starter.fullname" . }}
  labels:
    {{- include "go-starter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-starter.selectorLabels" . | nindent 4 }}
